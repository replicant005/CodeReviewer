version: "3.9"

services:

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"

  mcp-server:
    build: .
    container_name: code-guardian
    depends_on:
      - ollama
    environment:
      - OLLAMA_URL=http://ollama:11434/api/generate